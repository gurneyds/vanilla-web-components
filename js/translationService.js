(function() {
	console.log("Translation service starting");
	// Get the translation from the FS global variable on the page

	// Walk the DOM looking for elements that have the "translations" property and pass in


	console.log("Translation service finished");
})();
